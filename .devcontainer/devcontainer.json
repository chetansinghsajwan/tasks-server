{
  "name": "tasks-server-dev",
  "dockerComposeFile": "docker-compose.yml",
  "service": "server",
  "workspaceFolder": "/workspace",
  "forwardPorts": [
    5432, // for postgres
    8080 // for pgadmin
  ]
}
